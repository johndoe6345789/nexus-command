import { APP_VERSION } from './app'

export const INITIAL_CONSOLE_OUTPUT = [
  '> System initialized',
  `> Nexus Command ${APP_VERSION} - Developer Console`,
  '> Type "help" for available commands',
]
